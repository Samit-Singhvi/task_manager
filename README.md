# Task Manager

1. Install the requirements using requirements.txt
   `pip install -r requirements.txt`
2. Though sqlite db is used, but should delete it first - delete instance folder, and then run reset_db.py
3. Run the app -
   `flask run`
